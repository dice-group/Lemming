package org.aksw.simba.lemming.metrics.single.edgemanipulation;

/**
 * Enum for Graph operation during amendment phase.
 * ADD denotes add an edge operation.
 * REMOVE denotes remove an edge operation.
 *
 */
public enum Operation {
    ADD, REMOVE;
}
